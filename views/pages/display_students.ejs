<!DOCTYPE html>
<html lang="en">
  <head>
  	<title>display_students</title>
  	<link rel="stylesheet" href = "students.css">
  	<script src="stuDB.js"></script>
  </head>
      <body>
      <!-- <div id = "background"></div>
      <div id = "home"><a href="students.html">Home</a></div>
      <div id = "readme"><a href="readme.html">ReadMe</a></div>

        <h4 style="position: absolute;margin-top: 50px;margin-left: 50px;">
          Display Page<br>
        </h4>
        <div id = "background_div" style="border-style:solid;position:absolute;margin-top:100px;margin-left:50px;border-width:1px;">
          <table style="padding: 20px;position:absolute;">
            <% for(var row=0;row<=s.rows.length/5;row++)%>
            <tr>
              <% for(var i=row*5;i<(row+1)*5;i++){%>
                <td>
                  <% var scale=0.6; var ss=s.rows[i] %>
                  <% var div_style="width:"+ss.weight*scale+"px;height:"+ss.height*scale+"line-height:50px;margin:10px;border-width:1px;text-align:center;position:absolute;font-family:helvetica" %>
                  <div style= <%= div_style %>>
                    <font color= <%=ss.haircolor %> ><%= ss.name %></font><br> <%= ss.gpa %>
                  </div>
                </td>
              <% } %>
            </tr>
            <% } %>
          </table>
        </div> -->
    </body>
</html>
